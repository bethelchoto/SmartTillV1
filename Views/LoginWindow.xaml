<Window x:Class="LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartTill.V2"
        mc:Ignorable="d"
        Title="SmartTill Login" Height="500" Width="850"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <!-- Style for Placeholder TextBoxes -->
        <Style x:Key="PlaceholderTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True"/>
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            <TextBlock x:Name="textBlock" Text="{TemplateBinding Tag}" Foreground="LightGray" IsHitTestVisible="False" VerticalAlignment="Center" Margin="5,0,0,0" Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                    <Condition Property="IsFocused" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="textBlock" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" Background="#2C1A4D" ClipToBounds="True">
        <Grid>
            <!-- Window Layout -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Side: Branding -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image Source="/Assets/img/1-removebg-preview.png" Width="300" Margin="0,0,0,10"/>
                <TextBlock Text="Powered By Advanced POS Logic" FontSize="16" Foreground="#AAA" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Right Side: Login Form -->
            <Border Grid.Column="1" Background="#F5F5F5" CornerRadius="40,20,20,40">
                <Grid Margin="40">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Top Right Controls (Moved back from Top Left) -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,-20,-20,0" VerticalAlignment="Top">
                        <Button Content="—" Click="Minimize_Click" Width="30" Height="30" Background="Transparent" BorderThickness="0" Foreground="#888" Cursor="Hand" Margin="0,0,5,0"/>
                        <Button Content="✕" Click="Close_Click" Width="30" Height="30" Background="Transparent" BorderThickness="0" Foreground="#888" Cursor="Hand"/>
                    </StackPanel>

                    <TextBlock Grid.Row="0" Text="Login" FontSize="32" FontWeight="Bold" Foreground="#2C1A4D" HorizontalAlignment="Center" Margin="0,30,0,40"/>

                    <StackPanel Grid.Row="1" VerticalAlignment="Top">
                        <Border BorderBrush="#CCC" BorderThickness="1.5" CornerRadius="10" Background="White" Margin="0,0,0,20" Padding="15,10">
                            <TextBox x:Name="txtUsername" Text="{Binding Username}" Style="{StaticResource PlaceholderTextBox}" Tag="User Name"/>
                        </Border>

                        <Border BorderBrush="#CCC" BorderThickness="1.5" CornerRadius="10" Background="White" Margin="0,0,0,10" Padding="15,10">
                            <Grid>
                                <PasswordBox x:Name="txtPassword" BorderThickness="0" Background="Transparent" FontSize="16" VerticalAlignment="Center" PasswordChanged="txtPassword_PasswordChanged"/>
                                <TextBlock x:Name="txtPasswordWatermark" Text="Password" Foreground="LightGray" IsHitTestVisible="False" VerticalAlignment="Center" FontSize="16" Margin="5,0,0,0"/>
                            </Grid>
                        </Border>

                        <TextBlock Text="Forgot Password?" Foreground="#2C1A4D" FontWeight="SemiBold" HorizontalAlignment="Right" Cursor="Hand" Margin="0,0,10,20"/>
                        
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0,0,0,10" TextWrapping="Wrap"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                        <Button Content="Login" Click="Login_Click" Width="120" Height="45" Background="#2C1A4D" Foreground="White" FontWeight="Bold" Margin="0,0,15,0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="22">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Signup replaced with Cancel as requested -->
                        <Button Content="Cancel" Click="Close_Click" Width="120" Height="45" Background="#D11111" Foreground="White" FontWeight="Bold">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="22">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>