<UserControl x:Class="POSView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartTill.V2"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="White">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Column: Checkout Cart -->
        <Border Grid.Column="0" Background="#F0F2F5" CornerRadius="12" Margin="0,0,20,0" Padding="20">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Customer Receipt" FontSize="22" FontWeight="Bold" Foreground="#2C1A4D" Margin="0,0,0,20"/>
                
                <!-- Cart Actions / Total -->
                <Border DockPanel.Dock="Bottom" Background="White" CornerRadius="8" Padding="20" Margin="0,20,0,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,15">
                            <TextBlock Text="TOTAL DUE" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding TotalAmount, StringFormat=C2}" FontSize="32" FontWeight="Bold" Foreground="#2C1A4D" HorizontalAlignment="Right"/>
                        </Grid>
                        
                        <Button Content="PROCESS SALE" Height="60" Background="#2C1A4D" Foreground="White" FontSize="20" FontWeight="Bold" Command="{Binding CheckoutCommand}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        
                        <TextBlock Text="{Binding StatusMessage}" Foreground="Green" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Cart Items List -->
                <DataGrid ItemsSource="{Binding CartItems}" AutoGenerateColumns="False" CanUserAddRows="False" Background="Transparent" BorderThickness="0" GridLinesVisibility="None" RowHeight="45">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ITEM" Binding="{Binding Product.Name}" Width="*"/>
                        <DataGridTextColumn Header="QTY" Binding="{Binding Quantity}" Width="60"/>
                        <DataGridTextColumn Header="PRICE" Binding="{Binding Product.Price, StringFormat=C2}" Width="80"/>
                        <DataGridTextColumn Header="SUBTOTAL" Binding="{Binding Subtotal, StringFormat=C2}" Width="100"/>
                        <DataGridTemplateColumn Width="50">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="✖" Foreground="Red" Background="Transparent" BorderThickness="0" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <!-- Right Column: Product Search & Inventory -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search Bar -->
            <Border Grid.Row="0" Background="#2C1A4D" Padding="15" CornerRadius="12" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Product Search" Foreground="White" Margin="0,0,0,8" FontWeight="SemiBold"/>
                    <Grid>
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="10,8" VerticalContentAlignment="Center">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Inventory Grid -->
            <Border Grid.Row="1" Background="#F8F9FA" CornerRadius="12" Padding="10">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Inventory}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.AddItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="0,0,0,8">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="White" BorderBrush="#EEE" BorderThickness="1" CornerRadius="8" Padding="15">
                                                <Grid>
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="#2C1A4D"/>
                                                        <TextBlock Text="{Binding Category}" FontSize="12" Foreground="#888"/>
                                                        <TextBlock Text="{Binding Price, StringFormat=C2}" FontWeight="Bold" Margin="0,5,0,0" Foreground="#4CAF50"/>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding StockQuantity, StringFormat='Stock: {0}'}" HorizontalAlignment="Right" VerticalAlignment="Top" FontSize="11" Foreground="#999"/>
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>