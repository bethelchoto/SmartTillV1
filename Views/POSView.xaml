<UserControl x:Class="POSView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartTill.V2"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1200"
             Background="#F4F7FA">

    <UserControl.Resources>
        <DropShadowEffect x:Key="DropShadowEffect" BlurRadius="15" Direction="270" RenderingBias="Quality" ShadowDepth="1" Opacity="0.1"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Info Bar -->
        <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="20,15" Margin="0,0,0,20" Effect="{DynamicResource DropShadowEffect}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Sale #: NEW" FontWeight="Bold" Foreground="#2C1A4D" FontSize="16" VerticalAlignment="Center" Margin="0,0,30,0"/>
                    <TextBlock Text="{Binding Source={x:Static sys:DateTime.Now}, StringFormat=D}" Foreground="#888" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Customer:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Text="{Binding CustomerName}" Width="250" Padding="8,5" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Border Background="#E8F5E9" CornerRadius="15" Padding="12,5" VerticalAlignment="Center">
                        <TextBlock Text="Terminal Online" Foreground="#2E7D32" FontWeight="SemiBold" FontSize="12"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Terminal Body -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <!-- Checkout Grid (Left) -->
            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20" Margin="0,0,20,0">
                <DockPanel>
                    <!-- Quick Search / Barcode -->
                    <Border DockPanel.Dock="Top" Background="#F8F9FA" Padding="15,10" CornerRadius="8" Margin="0,0,0,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Stock Code / Barcode:" VerticalAlignment="Center" Margin="0,0,15,0" FontWeight="SemiBold"/>
                            <TextBox Grid.Column="1" Text="{Binding BarcodeInput, UpdateSourceTrigger=PropertyChanged}" Padding="10,8" FontSize="16">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding QuickAddCommand}"/>
                                </TextBox.InputBindings>
                            </TextBox>
                            <Button Grid.Column="2" Content="ADD" Command="{Binding QuickAddCommand}" Margin="10,0,0,0" Padding="20,0" Background="#2C1A4D" Foreground="White" FontWeight="Bold"/>
                        </Grid>
                    </Border>

                    <!-- Cart Grid -->
                    <DataGrid ItemsSource="{Binding CartItems}" AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="None" Background="Transparent" BorderThickness="0" RowHeight="55" FontSize="14">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="PRODUCT NAME" Binding="{Binding ProductName}" Width="*"/>
                            <DataGridTextColumn Header="QTY" Binding="{Binding Quantity}" Width="80"/>
                            <DataGridTextColumn Header="UNIT PRICE" Binding="{Binding UnitPrice, StringFormat=C2}" Width="120"/>
                            <DataGridTextColumn Header="DISC. %" Binding="{Binding DiscountPercent}" Width="80"/>
                            <DataGridTextColumn Header="TOTAL" Binding="{Binding Total, StringFormat=C2}" Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="#2C1A4D"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTemplateColumn Width="60">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Remove" Foreground="#D32F2F" Background="Transparent" BorderThickness="0" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <!-- Payment & Summary (Right) -->
            <Border Grid.Column="1" Background="White" CornerRadius="12" Padding="25">
                <DockPanel>
                    <!-- Bottom Actions -->
                    <StackPanel DockPanel.Dock="Bottom">
                        <Button Content="PROCESS SALE (F2)" Height="70" Background="#4CAF50" Foreground="White" FontSize="20" FontWeight="Bold" Command="{Binding CheckoutCommand}" Margin="0,0,0,10">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="HOLD / RECALL" Height="50" Margin="0,0,5,0" Background="#2196F3" Foreground="White" FontWeight="SemiBold"/>
                            <Button Grid.Column="1" Content="ABORT SALE" Height="50" Margin="5,0,0,0" Background="#F44336" Foreground="White" FontWeight="SemiBold" Command="{Binding AbortCommand}"/>
                        </Grid>
                    </StackPanel>

                    <!-- Financial Summary -->
                    <StackPanel>
                        <TextBlock Text="Payment Details" FontWeight="Bold" FontSize="18" Foreground="#2C1A4D" Margin="0,0,0,20"/>
                        
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Subtotal" Foreground="#888"/>
                            <TextBlock Text="{Binding Subtotal, StringFormat=C2}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                        </Grid>
                        
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Discount %" Foreground="#888" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding DiscountRate, UpdateSourceTrigger=PropertyChanged}" Width="80" HorizontalAlignment="Right" Padding="5,3" HorizontalContentAlignment="Right"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Tax (VAT 15%)" Foreground="#888"/>
                            <TextBlock Text="{Binding TaxAmount, StringFormat=C2}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                        </Grid>
                        
                        <Separator Margin="0,15"/>
                        
                        <Grid Margin="0,0,0,25">
                            <TextBlock Text="GRAND TOTAL" FontSize="18" FontWeight="Bold" Foreground="#2C1A4D"/>
                            <TextBlock Text="{Binding TotalAmount, StringFormat=C2}" HorizontalAlignment="Right" FontSize="24" FontWeight="Bold" Foreground="#2C1A4D"/>
                        </Grid>

                        <TextBlock Text="Payment Method" Margin="0,0,0,8" Foreground="#555"/>
                        <ComboBox ItemsSource="{Binding PaymentMethods}" SelectedItem="{Binding SelectedPaymentMethod}" Padding="10" Margin="0,0,0,20"/>

                        <TextBlock Text="Amount Paid" Margin="0,0,0,8" Foreground="#555"/>
                        <TextBox Text="{Binding AmountPaid, UpdateSourceTrigger=PropertyChanged}" FontSize="24" FontWeight="Bold" Padding="10" Foreground="#2E7D32"/>
                        
                        <Grid Margin="0,15,0,0">
                            <TextBlock Text="CHANGE DUE" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding ChangeDue, StringFormat=C2}" HorizontalAlignment="Right" FontSize="20" FontWeight="Bold" Foreground="#D32F2F"/>
                        </Grid>
                        
                        <TextBlock Text="{Binding StatusMessage}" Foreground="#D32F2F" Margin="0,20,0,0" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                    </StackPanel>
                </DockPanel>
            </Border>
        </Grid>

        <!-- Function Key Legend (Bottom) -->
        <Border Grid.Row="2" Background="#2C1A4D" CornerRadius="8" Padding="15,10" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="F1 Search Product" Foreground="White" Margin="0,0,30,0"/>
                <TextBlock Text="F2 Process Sale" Foreground="White" Margin="0,0,30,0"/>
                <TextBlock Text="F3 Print Last Receipt" Foreground="White" Margin="0,0,30,0"/>
                <TextBlock Text="ESC Clear All" Foreground="White"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>