<UserControl x:Class="ProductManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartTill.V2"
             xmlns:views="clr-namespace:SmartTill.V2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100"
             Background="White">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        
        <!-- Modern TabControl Style -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" Margin="0,0,5,0" Padding="25,12" Background="#F8F9FA" CornerRadius="6,6,0,0">
                            <ContentPresenter Name="Content" ContentSource="Header" TextElement.Foreground="#555" TextElement.FontWeight="SemiBold"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="White"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#DDD"/>
                                <Setter TargetName="Content" Property="TextElement.Foreground" Value="#2C1A4D"/>
                                <Setter TargetName="Content" Property="TextElement.FontWeight" Value="Bold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Inventory Management" FontSize="28" FontWeight="Bold" Foreground="#2C1A4D"/>
            <TextBlock Text="Configure your store categories and manage your product catalog." Foreground="#888" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Main Content Tabs -->
        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}">
            
            <!-- Category Management Tab -->
            <TabItem Header="Category Management">
                <Grid Margin="0,20,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Background="#F0F2F5" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Add New Category" FontWeight="Bold" FontSize="16" Margin="0,0,0,15" Foreground="#2C1A4D"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBox Width="300" Text="{Binding NewCategoryName, UpdateSourceTrigger=PropertyChanged}" Padding="10,8" VerticalContentAlignment="Center"/>
                                <Button Content="Save Category" Command="{Binding AddCategoryCommand}" Margin="15,0,0,0" Padding="25,0" Background="#2C1A4D" Foreground="White" FontWeight="Bold">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Background="White" BorderBrush="#EEE" BorderThickness="1" CornerRadius="8">
                        <DataGrid ItemsSource="{Binding Categories}" AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="None" RowHeight="50">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="CATEGORY NAME" Binding="{Binding Name}" Width="*"/>
                                <DataGridTemplateColumn Header="ACTIONS" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Delete" Foreground="#D11111" Background="Transparent" BorderThickness="0" 
                                                    Command="{Binding DataContext.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Product Management Tab -->
            <TabItem Header="Product Management">
                <Grid Margin="0,20,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Action Bar -->
                    <Border Grid.Row="0" Background="#F8F9FA" Padding="20,15" CornerRadius="8" Margin="0,0,0,20">
                        <Grid>
                            <Button Content="+ Add New Product" Background="#2C1A4D" Foreground="White" Padding="20,8" FontWeight="Bold" Command="{Binding ToggleAddFormCommand}" HorizontalAlignment="Left">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBox Width="250" Padding="10,5" VerticalContentAlignment="Center" d:Text="Search..."/>
                                <Button Content="Refresh" Margin="10,0,0,0" Padding="15,5" Command="{Binding RefreshCommand}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Add/Edit Product Form -->
                    <Border Grid.Row="1" Background="#F0F2F5" CornerRadius="8" Padding="20" Margin="0,0,0,25" Visibility="{Binding IsAddFormVisible, Converter={StaticResource BoolToVis}}">
                        <StackPanel>
                            <TextBlock Text="Product Details" FontWeight="Bold" FontSize="16" Margin="0,0,0,15" Foreground="#2C1A4D"/>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,10">
                                    <TextBlock Text="Product Name" Margin="0,0,0,5" Foreground="#555"/>
                                    <TextBox Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,15,10">
                                    <TextBlock Text="Barcode" Margin="0,0,0,5" Foreground="#555"/>
                                    <TextBox Text="{Binding NewBarcode, UpdateSourceTrigger=PropertyChanged}" Padding="10,8"/>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,10">
                                    <TextBlock Text="Category" Margin="0,0,0,5" Foreground="#555"/>
                                    <ComboBox ItemsSource="{Binding Categories}" SelectedValue="{Binding NewCategory}" SelectedValuePath="Name" DisplayMemberPath="Name" Padding="10,8"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,15,0">
                                    <TextBlock Text="Price" Margin="0,0,0,5" Foreground="#555"/>
                                    <TextBox Text="{Binding NewPrice, UpdateSourceTrigger=PropertyChanged, TargetNullValue=0, FallbackValue=0}" Padding="10,8"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,15,0">
                                    <TextBlock Text="Initial Stock" Margin="0,0,0,5" Foreground="#555"/>
                                    <TextBox Text="{Binding NewStock, UpdateSourceTrigger=PropertyChanged, TargetNullValue=0, FallbackValue=0}" Padding="10,8"/>
                                </StackPanel>
                            </Grid>
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                                <Button Content="Cancel" Command="{Binding ToggleAddFormCommand}" Margin="0,0,15,0" Padding="15,5"/>
                                <Button Content="Save Product" Command="{Binding SaveProductCommand}" Background="#2C1A4D" Foreground="White" Padding="25,8" FontWeight="Bold">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Product Table -->
                    <Border Grid.Row="2" Background="White" BorderBrush="#EEE" BorderThickness="1" CornerRadius="8">
                        <DataGrid ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="None" RowHeight="50">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="BARCODE" Binding="{Binding Barcode}" Width="150"/>
                                <DataGridTextColumn Header="PRODUCT NAME" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="CATEGORY" Binding="{Binding Category}" Width="150"/>
                                <DataGridTextColumn Header="PRICE" Binding="{Binding Price, StringFormat=C2}" Width="100"/>
                                <DataGridTextColumn Header="STOCK" Binding="{Binding StockQuantity}" Width="100" IsReadOnly="True"/>
                                <DataGridTemplateColumn Header="ACTIONS" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Delete" Foreground="#D11111" Command="{Binding DataContext.DeleteProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>